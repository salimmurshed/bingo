enum HomePageBottomTabs {
  dashboard,
  requests,
  settings,
  accountBalance,
}

enum HomePageRequestTabs {
  associateRequest,
  creditLineRequest,
}

enum HomePageSettingTabs {
  users,
  roles,
  stores,
  manageAccounts,
}
