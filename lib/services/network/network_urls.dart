import '../../app/app_config.dart';

class NetworkUrls {
  static String loginUrl = "${Config.baseURL}login";
  static String countryUrl = "${Config.baseURL}country-list";
  static String storeUrl = "${Config.baseURL}store-list";
}
